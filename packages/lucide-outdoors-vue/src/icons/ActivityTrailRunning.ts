import { createLucideIcon } from 'lucide-vue-next'
import { activityTrailRunning } from 'lucide-outdoors'

export const ActivityTrailRunning = createLucideIcon('od-activity-trail-running', activityTrailRunning)
