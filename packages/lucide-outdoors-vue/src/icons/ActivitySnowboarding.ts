import { createLucideIcon } from 'lucide-vue-next'
import { activitySnowboarding } from 'lucide-outdoors'

export const ActivitySnowboarding = createLucideIcon('od-activity-snowboarding', activitySnowboarding)
